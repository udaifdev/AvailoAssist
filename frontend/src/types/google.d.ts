// google.d.ts
declare global {
    interface Window {
      google: any; // Declare google as 'any' to bypass the type check for now
    }
  }
  
  export {}; // Ensure this file is treated as a module
  